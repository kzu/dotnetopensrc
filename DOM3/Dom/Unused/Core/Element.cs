using System;
using System.Xml;
using NMatrix.W3C.DOM.Core;

namespace NMatrix.NMatrix.Dom.Core
{
	public class Element : XmlElement, IElement
	{
		/*
		string TagName { get; }
		string GetAttribute(string name);
		void SetAttribute(string name, string value); //raises(DOMException);
		void RemoveAttribute(string name); //raises(DOMException);
		IAttr GetAttributeNode(string name);
		IAttr SetAttributeNode(IAttr newAttr); //raises(DOMException);
		IAttr RemoveAttributeNode(IAttr oldAttr); //raises(DOMException);
		INodeList GetElementsByTagName(string name);
		// Introduced in DOM Level 2:
		string GetAttributeNS(string namespaceURI, string localName);
		// Introduced in DOM Level 2:
		void SetAttributeNS(string namespaceURI, string qualifiedName, string value); //raises(DOMException);
		// Introduced in DOM Level 2:
		void RemoveAttributeNS(string namespaceURI, string localName); //raises(DOMException);
		// Introduced in DOM Level 2:
		IAttr GetAttributeNodeNS(string namespaceURI, string localName);
		// Introduced in DOM Level 2:
		IAttr SetAttributeNodeNS(IAttr newAttr); //raises(DOMException);
		// Introduced in DOM Level 2:
		INodeList GetElementsByTagNameNS(string namespaceURI, string localName);
		// Introduced in DOM Level 2:
		bool HasAttribute(string name);
		// Introduced in DOM Level 2:
		bool HasAttributeNS(string namespaceURI, string localName);
		*/
	}
}
